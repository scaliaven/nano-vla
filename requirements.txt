torch>=2.1
numpy==1.22.4 
transformers>=4.45
tqdm
Pillow
hydra-core==1.2.0
easydict==1.9 
opencv-python==4.6.0.66 
robomimic==0.2.0 
einops==0.4.1
thop==0.1.1-2209072238
robosuite==1.4.0
bddl==1.0.1
future==0.18.2 
matplotlib==3.5.3 
# cloudpickle==2.1.0
gym==0.25.2

h5py            # convert_libero.py (LIBERO HDF5 path)
pyarrow         # convert_libero_lerobot.py + data.LeRobotTrajectoryDataset (parquet)
imageio         # convert_libero_lerobot.py + data.LeRobotTrajectoryDataset (mp4 decode)
av              # data.LeRobotTrajectoryDataset (random-access mp4 decode via PyAV)

# Optional, lazy-imported — install only if you need them:
#   wandb        train.py logging (--wandb)
#   libero       eval_libero.py sim rollouts; pulls in robosuite + mujoco
#                https://github.com/Lifelong-Robot-Learning/LIBERO
